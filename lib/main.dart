import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    routes: {
      '/': (context) => const Placeholder(),
      '/semesters': (context) => const Placeholder(),
      '/calculator': (context) => const Placeholder(),
      '/results': (context) => const Placeholder(),
      '/criteria': (context) => const Placeholder(),
    }
  ));
}
